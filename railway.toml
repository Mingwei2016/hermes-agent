[build]
builder = "dockerfile"
dockerfilePath = "Dockerfile.railway"

[deploy]
startCommand = "hermes gateway"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Trigger rebuild
[environments.production.deploy]
numReplicas = 1